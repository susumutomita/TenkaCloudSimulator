{
  "name": "@tenkacloud/simulator-provider-aws",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "dev": "bun test --watch tests",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test tests",
    "test:coverage": "bun test --coverage tests",
    "typecheck": "tsc --noEmit",
    "verify:catalog": "bun scripts/verify-catalog.ts"
  },
  "dependencies": {
    "@tenkacloud/simulator-core": "workspace:*",
    "yaml": "2.8.2"
  }
}
